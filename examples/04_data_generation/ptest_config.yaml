# ptest 配置文件
# 数据生成测试示例

logging:
  level: INFO

reporting:
  enabled: true
  format: json
  output_dir: ./reports

execution:
  retry_failed: false
  parallel: false
  timeout: 30

# 数据生成配置
data_generation:
  default_count: 10
  default_format: json
  faker_locale: zh_CN
